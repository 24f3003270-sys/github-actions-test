name: Test Email Step

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: 24f3003270@ds.study.iitm.ac.in
        run: echo "Hello, world!"
